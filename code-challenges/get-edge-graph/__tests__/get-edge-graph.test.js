'use strict';

const Graph = require('../../../data-structures/graph/graph');
const getEdge = require('../get-edge-graph');

describe('getEdge', () => {
  const graph = new Graph();

  it('getEdge()', () => {
  });

});
